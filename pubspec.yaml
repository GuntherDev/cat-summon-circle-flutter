name: cat_summon_circle_flutter
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  retrofit: any
  get: any
  dio: any
  google_fonts: any
  flutter_native_splash: any
  cupertino_icons: ^1.0.2

flutter_native_splash:
  color: "#ffffcc"
  image: "assets/splash.png"
  android: true
  ios: true
  web: true

dev_dependencies:

flutter:

  uses-material-design: true

  assets:
    - assets/

